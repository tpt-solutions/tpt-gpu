# Changelog

All notable changes to this crate will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

> **Versioning note.** This crate is published to
> [crates.io](https://crates.io) as part of the TPT GPU workspace at `0.1.0`.
> `0.1.x` is pre-1.0, so the public API is **not yet guaranteed stable** and may
> change in a `0.2.0` release.

---
## [Unreleased]

Pending changes for the next crate release after `0.1.0`.

## [0.1.0]

### Added

- Initial release: Stable C ABI (	ptr_c) over 	pt-gpu-runtime: device create/allocate/free, host-device memcpy, kernel create/launch, synchronize, error accessors.
- Published as part of the TPT GPU `0.1.0` workspace release.

---

*For workspace-wide changes, see the [root CHANGELOG](../../CHANGELOG.md).*
